# NOTES · Pi-Agent SDK 深度精读

> 记录**用户教学偏好**与**工作笔记**的便签。目标与范围见 [MISSION.md](./MISSION.md)。

## 工作区说明

- 本课主题:**对着冬瓜《Pi-Agent SDK 深度教程》,按 10 章顺序精读一个生产级 Agent SDK(`pi`,v0.80.2)的设计与实现**。
- **grounding 源**:教程 markdown 本身,在 `../原始资料/dg-ai-notes/pi-agent/docs/typescript/`(TS 版 10 章)。
  pi SDK 真源码**不在本库内**——教程已是对 pi 源码的二次精读,本课是对教程的**忠实转译**。
  引用代码/行号时用教程的口径(如「教程 §5.1」「agent.ts:166」),不自称亲自读过 pi 源码。
- 微课产物在 `lessons/`,命名 `NNNN-dash-case-topic.html`,一节一窄主题;共享 `assets/style.css`(自包含,零外链)。
- 导航脊椎:`reference/chapter-map.html`(10 章 → 微课映射)。门户见 `index.html`(Phase 4 建)。
- 与 `agent开发/` 其它课的关系:这是**第四门子课**,与 `book-course/`(书精读57课)、`from-zero/`(入门14)、
  顶层 `lessons/`(四支柱进阶18)并列。前三门讲「Agent 是什么、怎么用、四支柱」,本课讲「一个真实 SDK 的源码怎么长的」。
  与 `learn-code/`(读 xv6 内核)是**同一种源码精读体裁**,对象从内核换成 Agent SDK。

## 用户教学偏好

- 决策(AskUserQuestion):范围=**全量建课**(10章→约32节);语言轨=**TypeScript**。
- (随会话补充:讲解粒度、类比风格、想重点覆盖哪些子系统等。)

## 工作笔记

- **课程编排(固定分配,合计 32 节)**:
  - ch01 开篇总览 → 0001–0003(3)
  - ch02 三层架构 → 0004–0006(3)
  - ch03 Agent Loop → 0007–0010(4,★核心最重)
  - ch04 模型调用 → 0011–0013(3,★核心)
  - ch05 工具系统 → 0014–0017(4,★核心)
  - ch06 消息系统 → 0018–0020(3,★核心)
  - ch07 事件驱动 → 0021–0023(3)
  - ch08 上下文工程 → 0024–0026(3)
  - ch09 上下文压缩 → 0027–0029(3)
  - ch10 会话管理 → 0030–0032(3)
- **进度(全部完成)**:Phase 0 落盘+骨架 ✅ · Phase 1 style.css+母版0001+脚手架+chapter-map ✅ · Phase 2 设计扇出(31节蓝图,10章并行,已落盘 specs/)✅ · Phase 3 生成扇出(批A 0002-0017 + 批B 0018-0032,共31节;pipeline 起草→对抗复核→按需修复;全 pass 0 需修)✅ · Phase 4 门户 index.html + chapter-map 全✅ + 全课核验 ✅。
- **核验结论(主循环亲验,不信 agent 自述)**:32/32 存在;span 全平衡;自包含(仅 ../assets/style.css);**零死链**(修了母版 0001 一处预猜 slug);31 节自测答案逐位==spec;代码块合计 119 个、`<>` 全转义;6 节 DOM 抽样(0001/0002/0009/0015/0021/0028/0032)控制台 0 错、无溢出、无实体泄漏、自测交互正确;反幻觉:全课版本号仅 v0.80.2、关键数字未篡改。
- **建法**(沿用本仓库 7 门课经验):金标准母版 0001 锁风格 → Workflow 10 章读者提取蓝图 → 分批扇出生成(起草不用 schema 直接 Write,规避副作用跳过)→ 每批磁盘+DOM 亲验。
- **反幻觉纪律**:代码区裸 `<` `>` 必须转义 `&lt;` `&gt;`(否则被浏览器当标签吃掉、静默不渲染);数字/设计理由忠实教程,绝对措辞须有出处。
